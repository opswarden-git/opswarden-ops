apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: ${TLS_ISSUER}
spec:
  acme:
    email: ${ACME_EMAIL}
    server: ${ACME_SERVER}
    privateKeySecretRef:
      name: ${TLS_ISSUER}-account-key
    solvers:
      - http01:
          ingress:
            ingressClassName: traefik
