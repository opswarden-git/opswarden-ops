apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: opswarden-api
  namespace: ${NAMESPACE}
  annotations:
    cert-manager.io/cluster-issuer: ${TLS_ISSUER}
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - ${API_HOST}
      secretName: opswarden-api-tls
  rules:
    - host: ${API_HOST}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: server
                port:
                  name: http
